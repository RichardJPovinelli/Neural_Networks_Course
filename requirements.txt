--extra-index-url https://download.pytorch.org/whl/cu125
# Use a version range so pip resolves a compatible CUDA wheel from the PyTorch index (no +cu suffix)
torch>=2.4,<3.0
transformers>=4.45.0
tokenizers>=0.20.0
numpy
matplotlib
requests
ipykernel